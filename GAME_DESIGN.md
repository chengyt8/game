# 🎠 乐园小游戏 · 游戏设计（GAME_DESIGN.md）

> 初稿：2026-08-01 · cc 与 Codex 协作完成；现由 Codex 全权维护。
> 唯一写者：Codex。根据水水最新反馈直接修订，并负责实现与验收。
> 献给水水：一个**操作性强的马里奥式轻快平台跳跃**小游戏。

---

## 0. 一句话

拼豆小牛在乐园里跑、跳、冲刺，收集彩豆，冲进终点门——L3 终点点亮摩天轮。

## 1. 核心循环（30 秒能懂、一关 2–3 分钟）

**跑 → 跳/冲刺越过地形与危险 → 收集彩豆与秘密 → 到达终点 → 按收集率得 1–3 星。**

## 2. 核心动作（v1 冻结）

| 动作 | 规则（调试起点，非不可变常量） |
|---|---|
| 左右移动 | 地面加速度 ~2400，最大跑速 ~300 px/s |
| 跳跃 | 初速 -640，**可变高度**（松手截断）；跳跃缓冲 0.12s；土狼时间 0.09s |
| 空中冲刺 | 每次滞空一次；420 px/s × 0.18s；落地重置；**8 向离散**：有方向输入时归一化到 8 向（含 4 斜向），无方向输入时按角色面向水平冲刺，冲刺期间方向锁定 |
| 重力 | ~1900 |
| 空中操控 | 地面的 ~85% |

> 参数是**调试起点**：cc 按体验验收提出调整，codex 按技术边界实现。

### 手感三件套（必做，属基础手感，不算玩法膨胀）
可变跳跃高度、跳跃缓冲、土狼时间。

### 程序化动作表现（v1 用代码做，不依赖新美术）
跳跃压扁/拉伸、奔跑前倾、落地扬尘粒子、冲刺拖影（鬼影帧）。
2–4 个拼豆姿势帧（待机/跑/跳/冲刺）列为**首版之后**的润色任务，不阻塞核心闭环。

## 3. 节奏：马里奥式轻快探索收集

- 主路线**宽容**（容错高）；秘密放在需要技巧的**支路**，区域内奖励在发现前不绘制，避免远处直接泄露位置。
- **重生点**：每关起点 + 1 个中段存档旗（触旗激活）。死亡**立即回到最近激活的存档旗**，不载入、不惩罚。
- **收集保留**：本次尝试内已收集的彩豆、已「发现」的秘密状态**保留不清零**（宽容原则）。
- 不用惩罚性精确房间；目标是「冲过去 + 捡满」，轻松爽快。

## 4. 三关教学结构

| 关 | 教学内容 | 新元素 |
|---|---|---|
| L1 入口广场 | 移动、跳跃、跳跃缓冲与边缘宽容 | 彩豆、终点门；**终点前自然拿到空中冲刺** |
| L2 云上步道 | 用空中冲刺跨长距离 | 支路「冲刺后接跳」的秘密彩豆、更高危险区 |
| L3 摩天轮之夜 | 组合：移动平台 + 弹簧 + 冲刺 | **一只可踩踏的笨敌人**；终点点亮摩天轮 |

> 每关至少 1 个秘密区域，藏在需要技巧的支路上。未发现时区域无边框、无标记，内部彩豆与承载它们的高架都隐藏且不参与碰撞；进入后短暂显形并揭示高架与奖励。模糊线索牌保留，不直接泄露位置。

## 5. 实体与危险（v1 范围）

| 实体 | 行为 |
|---|---|
| 静态平台 / 移动平台 | 承载玩家；移动平台在范围内往返 |
| 弹簧 | 触碰向上弹起 |
| 危险地形（尖刺 / 深渊） | 接触或掉落即死，回到最近激活的存档旗 |
| 存档旗（每关 1 个中段） | 触旗激活为重生点 |
| 彩豆（收集物） | 主路线 + 支路秘密；吃满得高星 |
| 终点门 | 到达过关（L3 为点亮摩天轮动画） |
| **敌人（仅 L3、仅一种）** | 水平巡逻、到边缘折返、**踩踏消灭**、侧碰受伤重生；**无攻击按钮、无变种、无复杂 AI** |

## 6. 胜负与计分

- **过关**：到达终点门（L3 为点亮摩天轮）。
- **星级**：过关 = 1 星；彩豆收集 ≥80% = 2 星；收集 100% **且** 进入 ≥1 个秘密区域 = 3 星（「发现秘密」= 进入隐藏触发区，触发显形、专属音效和 HUD 计数，不要求当场吃光该区彩豆）。
- **存档**：localStorage 记录每关最高星级与进度（技术方案由 codex 定）。

## 7. 操作方案（首版）

- 键盘：`←→` / `AD` 移动，`Space` / `↑` / `W` 跳，`J` / `Shift` 冲刺，`R` 重开本关，`M` 静音，`Esc` / `P` 暂停。
- 触屏：左下虚拟方向键，右下 `跳` / `冲刺` 两按钮；同样支持暂停/重开。
- 无障碍基础：音量开关、配色可读性检查。

## 8. 首版明确不做

- 复杂敌人 AI / 战斗系统 / 敌人变种
- 随机生成、关卡编辑器
- 多姿势帧动画（列为首版后润色）
- 网络请求、CDN、远程字体（**离线是硬约束**）

## 9. 内容与代码边界

- 内容（关卡/图案/数值/文案/色板）是**数据**，注册到 `Park.content`；内容与运行时均由 Codex 统一维护。
- 加内容（新关/新图案）原则上**只加数据、不改核心**——此约束写入架构。

## 10. 体验验收标准

- [ ] 首次游玩 30 秒内可独立上手（无需读说明）。
- [ ] 手感三件套生效：跳跃可变、缓冲/土狼时间可感知。
- [ ] 每关主路线 2–3 分钟内可达终点；死亡重生 <1 秒。
- [ ] 空中冲刺在 L2/L3 有「用得上且好玩」的场景。
- [ ] L3 敌人行为符合边界（巡逻/折返/踩踏/侧碰），无越界。
- [ ] 离线 `file://` 双击可玩；**键盘与触屏均能通关 L1–L5**。
- [ ] 触屏专项：L2 触屏斜向冲刺无明显误触。

## 11. 扩展记录（首版冻结后的变更，按时间追加，不改历史决策）

- 2026-08-01 · **L1 难度回调**：移除 L1 尖刺（水水实玩反馈存档点后太紧；L1 是纯移动跳跃教学关，尖刺由 L2/L3 承担）。
- 2026-08-01 · **弹簧修复**：L3 弹簧对齐高台正下方 + 弹道彩豆；L2 加弹道彩豆与提示牌（水水反馈「弹簧不知道有啥用」）。
- 2026-08-01 · **关卡扩展到 5 关**：新增 L4「齿轮工坊」（dusk 黄昏，精确跳跃/尖刺走廊/移动平台+尖刺）、L5「不夜城」（night 终章，两只 walker + 组合机关）。敌人上限：L1/L2/L4=0、L3=1、L5=2；终章 = manifest 最后一关（自动灯牌与庆祝）。
- 2026-08-01 · **游戏内操作说明**：主菜单与暂停菜单显示完整操作表；明确「无二段跳，一次滞空一跳一冲」。

## 12. 无尽模式「无尽坠落」（2026-08-01 玩法修订）

> 玩法：浮动楼梯持续上升，小牛站在楼梯上，横移离开边缘后落到下一层；速度缓慢递增；按存活时间 + 彩豆计分。
> 入口：主菜单「无尽坠落」，通关 L1 后解锁。玩法、内容、运行时与验收统一由 codex 负责。

### 核心规则
- 楼梯是独立的单向承重平台，可位于左侧、中间或右侧，同层允许 1–2 块；相邻层纵距为 3–4 格。小牛开局站在第一层，横移离开平台边缘后失去支撑，并可落在下一层。
- 站在同一层过久会被楼梯顶出屏幕顶部；没有落到下一层、掉出屏幕底部也会结束。接触楼梯本身不会受伤。
- 部分楼梯带尖刺，踩中立即结束；部分楼梯水平往返并携带小牛、尖刺和彩球。同向移动时平台位移与玩家输入叠加，逆向时相互抵消。
- 首版边界（冻结）：缺口平台 + 彩豆 + 速度递进 + 生存/彩豆计分 + 最高分 + 结束/重开。**无**尖刺、机关、连击、道具、排行榜。
- 操作：`← →` 移动、`Space / ↑ / W` 从楼梯起跳、`↓` 在空中加速下落、水平冲刺。空中不能连续跳，落到楼梯后恢复跳跃并重置冲刺；触屏显示「跳」和「冲」。

### 段落与生成（数据契约）
- 段落数据：`ParkContent.modes.endless.segments`。
- 段落为 **16 列格网**；每段 `{ id, height, rows[] }`；每层 `{ y, platforms[], beans[] }`。
- 平台结构：`{ x, w, motion?: { distance, speed, phase }, spikes?: [{ offset, w }] }`；彩球结构：`{ platform, offset }`，绑定本层平台并随电梯移动。
- 每层 1–2 块、每块至少 3 格宽；相邻层纵距 3–4 格。电梯移动终点与尖刺必须完整留在 16 列场地和所属平台内。

### 数值（体验调参归内容侧，由 endless.js 提供）
- `speed: { initial: 42, increasePerSecond: 1.35, max: 150 }`（px/s），比首版 `80→320` 显著放慢。
- `scoring: { survivalPerSecond, beanPoints }`。
- 计分 = 存活时间 × survivalPerSecond + 彩豆 × beanPoints；最高分存 `bestEndlessScore`。

### 验收
- 30 秒内理解“站稳、找缺口、落下一层”；速度可感知递增；顶部/底部两种失败清晰；彩豆可捡、计分正确；死亡/重开闭环；触屏可玩；离线 `file://` 可用。
- [ ] 全收集与秘密有可辨识的正反馈（音效 + 特效 + 星级）。
- [ ] 音效可静音；无致命 bug（卡死 / 穿墙 / 无法过关）。
